import React from 'react'


type LoopsProps = {
    loops: any[],
    error: any,
    loading: boolean
}
export default function Loops({ loops, error, loading }: LoopsProps) {
  return (
    <section className='flex-1 bg-black/10'>
        
    </section>
  )
}
